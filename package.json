{
  "name": "closetube",
  "version": "1.0.0",
  "description": "CloseTube - Private Video Sharing SaaS",
  "private": true,
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  },
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm start",
    "dev": "cd frontend && npm run dev"
  },
  "workspaces": [
    "frontend"
  ]
} 